[build]
cmd = "composer install --optimize-autoloader --no-dev && php artisan key:generate --force && php artisan config:cache && php artisan route:clear && php artisan route:cache && php artisan view:cache"

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

[env]
PORT = "8080"
